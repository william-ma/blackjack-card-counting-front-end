import React from 'react';
import ReactDOM from 'react-dom';

class Game extends React.Component {
    constructor(props) {
        super(props)
    }

    // tabs with multiple tables...
    // tables interactive... keep/hold the state? no idea...
    // there's no persistence because i CBF... writing the API for this just yet... but we'll play around...
}